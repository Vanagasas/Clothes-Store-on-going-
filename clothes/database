DROP DATABASE clothes;
CREATE DATABASE clothes;
USE clothes;
CREATE TABLE users (
    id INT AUTO_INCREMENT PRIMARY KEY,
    email VARCHAR(255) NOT NULL UNIQUE,
    password VARCHAR(255) NOT NULL,
    fullname VARCHAR(255) NOT NULL,
    adress TEXT NOT NULL,
    city VARCHAR(100) NOT NULL
);
CREATE TABLE clothes (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(255) NOT NULL,
    description TEXT,
    price DECIMAL(10, 2) NOT NULL,
    stock INT DEFAULT 0,
    img_url VARCHAR(255)
);
CREATE TABLE orders (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    order_date TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    total DECIMAL(10, 2) NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(id)
);
CREATE TABLE order_items (
    id INT AUTO_INCREMENT PRIMARY KEY,
    order_id INT NOT NULL,
    clothes_id INT NOT NULL,
    quantity INT NOT NULL,
    price DECIMAL(10, 2) NOT NULL,
    FOREIGN KEY (order_id) REFERENCES orders(id),
    FOREIGN KEY (clothes_id) REFERENCES clothes(id)
);
CREATE TABLE wishlist (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    clothes_id INT NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (clothes_id) REFERENCES clothes(id)
);
CREATE TABLE cart (
    id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    clothes_id INT NOT NULL,
    quantity INT NOT NULL,
    FOREIGN KEY (user_id) REFERENCES users(id),
    FOREIGN KEY (clothes_id) REFERENCES clothes(id)
);
INSERT INTO clothes (name, description, price, stock) VALUES
('Classic White T-Shirt', 'A timeless white t-shirt made from 100% cotton.', 19.99, 50),
('Blue Denim Jeans', 'Comfortable blue denim jeans with a modern fit.', 49.99, 30),
('Red Hoodie', 'Warm red hoodie with a front pocket and adjustable hood.', 39.99, 20),
('Black Leather Jacket', 'Stylish black leather jacket for a sleek look.', 89.99, 15),
('Summer Dress', 'Light and airy summer dress perfect for warm weather.', 29.99, 25);
